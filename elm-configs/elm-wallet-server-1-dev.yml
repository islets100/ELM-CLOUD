logging:
  level:
    '[org.springframework]': debug
    '[team.tjusw.mapper]': debug
    '[org.springframework.boot.autoconfigure]': error

spring:
  application:
    name:   elm-wallet-server
  cloud:
    config:
      enabled: false
  datasource:
    username: root
    password: Yb091864
    url: jdbc:mysql://localhost:3306/elm?characterEncoding=utf-8&useSSL=false&serverTimezone=UTC
    driver-class-name: com.mysql.cj.jdbc.Driver
mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: team.tjusw.po

redisClientConfig:
  host: localhost
  port: 6379
  timeout: 10000   #ms

eureka:
  client:
    service-url:
      defaultZone: http://localhost:13000/eureka/
  instance:
    prefer-ip-address: true
    instance-id: ${spring.cloud.client.ip-address}:${server.port}
