<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="team.tjusw.elm.mapper.DeliveryAddressMapper">
	<update id="updateDeliveryAddress" parameterType="team.tjusw.elm.po.DeliveryAddress">
		update deliveryAddress set valid = 0 where daId = #{daId}
	</update>
</mapper>
